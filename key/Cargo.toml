[package]
name = "pea-key"
version = "0.5.0"
edition = "2021"
license = "MIT"
description = "エンドウ"
repository = "https://github.com/peacash/peacash"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
secp256k1 = { version = "0.25.0", features = ["rand-std","recovery","global-context"] }
vrf = { version = "0.2.4", optional = true }
hex = "0.4.3"
rand = "0.8.5"
sha2 = "0.10.6"

[features]
vrf = ["dep:vrf"]