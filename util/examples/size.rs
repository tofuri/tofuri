use tofuri_util::EMPTY_BLOCK_SIZE;
use tofuri_util::STAKE_SIZE;
use tofuri_util::TRANSACTION_SIZE;
fn main() {
    println!("EMPTY_BLOCK_SIZE {}", *EMPTY_BLOCK_SIZE);
    println!("TRANSACTION_SIZE {}", *TRANSACTION_SIZE);
    println!("STAKE_SIZE {}", *STAKE_SIZE);
}
